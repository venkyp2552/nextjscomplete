import React from 'react'

//this will work simple localhost:3000/user(user folder name will act as route itself)
const page = () => {
    return (
        <div>Checking Page Rounting....</div>
    )
}

export default page